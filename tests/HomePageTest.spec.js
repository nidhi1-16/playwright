// const {test, expect} = require('@playwright/test')

// test('Home Page', async ({page})=> {
//    await page.goto('https://mcstaging.larsonjuhl.co.uk/');

//    const pageTitle = page.title();
//    console.log('Page title is :',pageTitle);
//    await expect(page).toHaveTitle('Picture Framing Suppliers & Wholesalers | Larson-Juhl UK');  //validating title of page
   
//    const pageURL = page.url();   //capturing the page URL
//    console.log('Page URL is : ',pageURL);
   
//    await expect(page).toHaveURL('https://mcstaging.larsonjuhl.co.uk/'); //validating url of the page
//    await page.close();

// })
